kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
kubeadmConfigPatches:
- |
  kind: ClusterConfiguration
  apiServer:
    certSANs:
    - "127.0.0.1"
    - "localhost"
    - "192.168.15.25"   # Mac LAN IP — update if your IP changes
nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker